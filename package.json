{
  "name": "focusflow",
  "version": "1.0.0",
  "description": "Deep work, distilled. Local-first Pomodoro PWA with task-linked timers, streak analytics and ambient sound. Zero dependencies, offline-first.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/",
    "check": "node scripts/check.js",
    "validate": "node scripts/validate.js",
    "serve": "node scripts/serve.js",
    "verify": "node scripts/check.js && node --test tests/ && node scripts/validate.js"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.1",
    "@capacitor/local-notifications": "^6.1.3"
  },
  "devDependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1"
  }
}
